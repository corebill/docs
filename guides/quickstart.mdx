---
title: "Quickstart"
description: "Make your first API call to Corebill in under 2 minutes."
---

## Prerequisites

1. A Corebill account at [app.corebill.io](https://app.corebill.io)
2. At least one company created in your organization
3. An API key (generate one in **Settings > Developers**)

## Step 1: Get your companies

First, retrieve your available companies to get the `company_id` you'll use in subsequent requests.

<CodeGroup>
```bash cURL
curl "https://api.corebill.io/v1/companies" \
  -H "Authorization: Bearer sk_live_your_api_key"
```

```javascript Node.js
const response = await fetch('https://api.corebill.io/v1/companies', {
  headers: {
    'Authorization': 'Bearer sk_live_your_api_key'
  }
});
const { data } = await response.json();
console.log(data.companies);
```

```python Python
import requests

response = requests.get(
    'https://api.corebill.io/v1/companies',
    headers={'Authorization': 'Bearer sk_live_your_api_key'}
)
companies = response.json()['data']['companies']
```
</CodeGroup>

**Response:**
```json
{
  "data": {
    "companies": [
      {
        "id": "com_a1b2c3d4e5f6",
        "name": "My Company",
        "slug": "my-company",
        "currency": "USD"
      }
    ]
  }
}
```

Copy the `id` value -- you'll need it for all other endpoints.

## Step 2: Create a customer

<CodeGroup>
```bash cURL
curl -X POST "https://api.corebill.io/v1/customers?company_id=com_a1b2c3d4e5f6" \
  -H "Authorization: Bearer sk_live_your_api_key" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Acme Corp",
    "country": "US",
    "email": "billing@acme.com",
    "currency": "USD"
  }'
```

```javascript Node.js
const response = await fetch(
  'https://api.corebill.io/v1/customers?company_id=com_a1b2c3d4e5f6',
  {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer sk_live_your_api_key',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      name: 'Acme Corp',
      country: 'US',
      email: 'billing@acme.com',
      currency: 'USD'
    })
  }
);
const { data } = await response.json();
```
</CodeGroup>

## Step 3: Create an invoice

<CodeGroup>
```bash cURL
curl -X POST "https://api.corebill.io/v1/invoices?company_id=com_a1b2c3d4e5f6" \
  -H "Authorization: Bearer sk_live_your_api_key" \
  -H "Content-Type: application/json" \
  -d '{
    "customer_id": "cus_a1b2c3d4e5f6",
    "due_date": "2025-05-15",
    "tax_rate": 16,
    "items": [
      {
        "name": "Web Development",
        "description": "Frontend implementation",
        "quantity": 40,
        "unit": "hour",
        "unit_price": 75.00
      },
      {
        "name": "UI Design",
        "quantity": 1,
        "unit": "project",
        "unit_price": 2000.00
      }
    ]
  }'
```
</CodeGroup>

**Response:**
```json
{
  "data": {
    "id": "inv_x1y2z3a4b5c6",
    "invoice_number": "INV-2025-000001",
    "status": "draft",
    "subtotal": 5000.00,
    "tax_rate": 16,
    "tax_amount": 800.00,
    "total": 5800.00,
    "amount_due": 5800.00,
    "items": [...]
  }
}
```

## Next steps

<Columns cols={2}>
  <Card title="Create Quotes" icon="file-lines" href="/guides/quotes">
    Send quotes and convert them to invoices.
  </Card>
  <Card title="Track Projects" icon="diagram-project" href="/guides/projects">
    Manage projects with tasks and time tracking.
  </Card>
</Columns>
